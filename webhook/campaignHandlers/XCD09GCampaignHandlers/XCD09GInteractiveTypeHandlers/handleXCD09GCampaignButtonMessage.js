export default async (req, res) => {
  const { contact, action, waClient } = res.locals;
  switch (action) {
    case "refer":
      break;
    default:
      break;
  }
};

/*global console, setTimeout*/
